import { createStyles} from '@mantine/core';

export const useCategoriesStyles = createStyles((theme) => ({

    cardButton: {
        textAlign:'center',
        fontSize: '0.75rem',
    },

    heroFont: {
        lineHeight: 'normal',
        textAlign: 'left',
    },


}))