import { createStyles } from "@mantine/core";

const useSendEmailModalStyles = createStyles((theme) => ({
  modalStyle: {
    position: "relative",
    zIndex: 2000,
  },
}));

export default useSendEmailModalStyles;
